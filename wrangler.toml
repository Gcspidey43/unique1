name = visa
compatibility_date = "2024-01-01"

# Build settings
[build]
command = "bun run build"

# Deployment settings
[deploy]
command = "npx wrangler pages deploy"

# Route settings
[route]
# No custom route configuration needed - Cloudflare handles this automatically

# Dev settings
[dev]
command = "npx wrangler pages dev"
